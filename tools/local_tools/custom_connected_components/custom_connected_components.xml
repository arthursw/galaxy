<?xml version="1.0"?>
<tool id="connected_components" name="Connected Components" version="1.0.0">
    <description>Compute connected components in the given binary image</description>

    <requirements>
        <requirement type="package" version="2.3.1">simpleitk</requirement>
    </requirements>

    <command detect_errors="exit_code">
        <![CDATA[
        python3 $__tool_directory__/custom_connected_components.py \
            --image '${image}' \
            --labeled_image '${labeled_image}'
        ]]>
    </command>

    <inputs>
        <param name="image" type="data" format="tiff" label="Input image" help="Binary input image"/>
    </inputs>

    <outputs>
        <data name="labeled_image" format="tiff" label="Labeled image"/>
    </outputs>

    <help><![CDATA[
This tool computes connected components on the input binary image using SimpleITK.

**Input**  
- A binary image (e.g., .nii, .nii.gz, .mha, .nrrd).

**Output**  
- A labeled image (16-bit integer).
    ]]></help>

    <citations/>
</tool>